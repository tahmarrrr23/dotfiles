#!/bin/sh
set -eu
cd "$(dirname "$0")"
root="$(dirname "$(pwd)")"
[ ! -d "/opt/homebrew" ] && echo "please install homebrew" && exit 1

# backup
/opt/homebrew/bin/brew bundle dump \
  --brews --casks --taps \
  --no-lock \
  --force \
  --file="$root/Brewfile" \
  -d
