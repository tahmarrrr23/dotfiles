#!/bin/sh
set -eu
cd "$(dirname "$0")"
[ -d "/opt/homebrew" ] && eval "$(/opt/homebrew/bin/brew shellenv)"

brew bundle --file="../Brewfile" --cleanup

"$(brew --prefix)"/opt/fzf/install --no-bash --no-fish --all
